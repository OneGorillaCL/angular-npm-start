import { GaDirective } from './ga.directive';

describe('GaDirective', () => {
  it('should create an instance', () => {
    const directive = new GaDirective();
    expect(directive).toBeTruthy();
  });
});
