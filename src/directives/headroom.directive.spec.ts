import { HeadroomDirective } from './headroom.directive';

describe('HeadroomDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadroomDirective();
    expect(directive).toBeTruthy();
  });
});
