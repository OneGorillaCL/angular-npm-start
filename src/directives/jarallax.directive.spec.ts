import { JarallaxDirective } from './jarallax.directive';

describe('JarallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new JarallaxDirective();
    expect(directive).toBeTruthy();
  });
});
